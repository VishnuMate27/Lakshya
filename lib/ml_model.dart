import 'package:flutter/material.dart';

class MlModelScreen extends StatelessWidget {
  const MlModelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
